@using Humanizer; 
@model IEnumerable<WebApplication.Models.DataFactory>

@{
    ViewData["Title"] = "Index - " + "WebApplication.Models.DataFactory".Pluralize().Humanize(LetterCasing.Title);
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1> @("WebApplication.Models.DataFactory".Pluralize().Humanize(LetterCasing.Title))</h1>

<p>
    <a class="btn-sm btn-dark details" asp-action="Create">Create New</a>
</p>
<table class="table table-striped">
    <thead class="thead-dark">
        <tr>
            <th></th>
             <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResourceGroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubscriptionUid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DefaultKeyVaultUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LogAnalyticsWorkspaceId)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn-sm btn-warning details" title="Edit"><i class="fa fa-pencil-square-o"></i></a> |
                <a asp-action="Details" asp-route-id="@item.Id" class="btn-sm btn-info details" title="Details"><i class="fa fa-list-alt" aria-hidden="true"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn-sm btn-danger details" title="Delete"><i class="fa fa-trash"></i></a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResourceGroup)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubscriptionUid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultKeyVaultUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LogAnalyticsWorkspaceId)
            </td>
        </tr>
}
    </tbody>
</table>
