@using Humanizer;
@model WebApplication.Models.ScheduleMaster

@{
    ViewData["Title"] = "Details - " + "ScheduleMaster".Humanize(LetterCasing.Title);
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4> @("ScheduleMaster".Humanize(LetterCasing.Title))</h4>
    <hr />
    <dl class="row">
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.ScheduleDesciption)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.ScheduleDesciption)
                </dd>               
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.ScheduleCronExpression)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.ScheduleCronExpression)
                </dd>               
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.ActiveYn)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.ActiveYn)
                </dd>               
    </dl>
</div>
<div>
    <a href="javascript:history.go(-1)" title="Back to List" class="btn btn-secondary"><i class="fa fa-arrow-circle-left"></i> Back</a>
    <a asp-action="Edit" asp-route-id="@Model.ScheduleMasterId" class="btn btn-warning details" title="Edit"><i class="fa fa-pencil-square-o"></i> Edit</a> 
</div>
@section Scripts
{
    <script type="text/javascript">
		$(document).ready(function ()
        {
            $(".jsonpretty").each(function (index) {
                $(".jsonformatted", this).JSONView($(".jsonraw", this)[0].innerText);
                $(".jsonraw", this)[0].innerText = null;
            });

		});
    </script>
}   
